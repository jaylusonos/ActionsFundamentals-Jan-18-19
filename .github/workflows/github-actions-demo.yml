name: GitHub Actions Demo

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '15 6 * * 0'

jobs:
  Build:
    runs-on: [ ubuntu-latest ]
    steps:
      - run: echo ${{ github.event_name }}
      - run: echo ${{ github.ref }}
      - uses: actions/checkout@v3.3.0
      - run: tree
